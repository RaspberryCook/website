<h1>users#index</h1>

<ul class="users">
	<% @users.each do |user| %>
		<li><%= render 'shared/user' , :user => user %></li>
	<% end %>
</ul>

<%= will_paginate @recipes , :class => "apple_pagination" %>