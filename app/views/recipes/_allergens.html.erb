<% if recipe.allergens %>
  <% recipe.allergens.each do |allergen| %>
    <%= image_tag allergen.icon_url, size: '50', alt: allergen.name, title: "Cette recette est #{allergen.name}" %>
  <% end %>
<% end %>